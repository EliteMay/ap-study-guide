# AP Study Notes 作業報告書

GitHub `EliteMay/ap-study-notes` を正本とする変更記録です。

## 運用ルール

- ChatGPTからGitHubを直接確認・修正する。
- Codex / ZIPを通常フローにしない。
- localStorage、用語ID、manifest、過去問対応を壊さない。
- 未確認事項を確認済みと書かない。
- 語数を教材品質の指標にしない。

---

# 1. 教材再設計の出発点

旧サイトを再監査し、完成判定を撤回した。

主な問題:

- 詳細解説のテンプレート比率が高い。
- 旧6分野だけではAP全範囲をカバーしない。
- 分野間に重複・誤配置がある。
- 計算・SQL・コード・図・ケース問題が弱い。
- 現行4択テストは用語認識中心。

# 2. カリキュラム再設計

IPA「応用情報技術者試験 シラバス Ver.7.2」を基準に、内部9大分類・23中分類、表示13学習ユニットへ再設計した。

追加:

- `json/curriculum/ap-2026-map.json`
- `html/roadmap.html`
- `js/roadmap.js`
- `css/roadmap.css`

# 3. 構造化Lesson基盤

追加:

- `html/lesson.html`
- `js/lesson.js`
- `css/lesson.css`
- `json/lessons/lesson-index.json`

主なsection:

- text
- comparison
- diagram
- code-trace
- steps
- mistakes
- worked-example renderer

Lessonナビは同じunit内をorder順で移動する。

# 4. Algorithm 65語移行

監査:

`json/curriculum/audits/algorithm-audit.json`

結果:

- keep-core 31
- keep-supporting 5
- merge 25
- move 4

実装:

- ALG-01〜11
- PROG-01〜04
- FND-01

旧65語は **65/65** を一意割当済み。

# 5. System 75語 + Management 72語移行

監査:

- `json/curriculum/audits/system-audit.json`
- `json/curriculum/audits/management-audit.json`

System:

- core 21
- supporting 6
- merge 44
- move 4

Management:

- core 18
- supporting 6
- merge 30
- move 18

主な再配置:

- RFP / フィージビリティ → システム企画
- SLA / ITSM → サービス管理
- Agile / Scrum → ソフトウェア開発管理技術
- コンテナ → コンピュータシステム

実装:

- SYS-01〜08
- DEV-01
- PM-01〜06
- SVC-01
- AUD-01
- STR-01
- CMP-01

System 75/75 + Management 72/72 = **147/147** を実Lessonへ一意割当済み。

# 6. Database 229語 全件監査

今回の大バッチで既存Database 229語を全件監査した。

追加:

`json/curriculum/audits/database-audit.json`

大規模データのため229件の冗長なdecision形式ではなく、14Lesson単位の `assignmentGroups[]` を採用。

結果:

| 判定 | 件数 |
|---|---:|
| core | 84 |
| supporting | 76 |
| merge | 69 |
| move | 0 |

重要な発見:

- SELECT / FROM / WHEREなどSQL構文が細かく単語カード化されすぎていた。
- キー関連も単独定義が多く、設計問題との接続が弱かった。
- `プリント対応パターン` は正式な分類ではなく教材出典固有の例だった。
- 旧詳細229件は同型テンプレートが多く、主教材として使う品質ではなかった。
- 229語すべての主分類はIPA中分類9「データベース」でよく、他ユニット移動は不要だった。

# 7. Database 14Lesson実装

新規:

- `json/lessons/database/db-01-dbms-relational-model.json`
- `json/lessons/database/db-02-er-keys-integrity.json`
- `json/lessons/database/db-03-normalization.json`
- `json/lessons/database/db-04-sql-definition-update.json`
- `json/lessons/database/db-05-select-null.json`
- `json/lessons/database/db-06-joins.json`
- `json/lessons/database/db-07-aggregate-set.json`
- `json/lessons/database/db-08-subquery-exists-cte.json`
- `json/lessons/database/db-09-transaction-isolation.json`
- `json/lessons/database/db-10-recovery-backup.json`
- `json/lessons/database/db-11-index-performance.json`
- `json/lessons/database/db-12-view-procedure-safe-sql.json`
- `json/lessons/database/db-13-distributed-nosql.json`
- `json/lessons/database/db-14-dwh-olap.json`

内容:

## DB-01

- DBMS / RDBMS
- 3層スキーマ
- 論理/物理データ独立性
- 関係 / タプル / 属性 / ドメイン
- 関係代数の選択 / 射影 / 結合を補完

## DB-02

- E-R図
- 主キー / 候補キー / スーパーキー / 外部キー
- 1:1 / 1:N / M:N
- 中間表
- 参照整合性のINSERT/DELETE判定

## DB-03

- 関数従属性
- 第1〜第3正規形
- 部分 / 推移的関数従属
- 更新 / 挿入 / 削除異常
- 非正規化

## DB-04〜08 SQL

- DDL / DML / DCL / TCL
- CREATE TABLEと制約
- SELECT論理評価順
- LIKE / BETWEEN / IN
- NULL三値論理
- CASE / COALESCE
- INNER / OUTER / CROSS / 自己結合
- GROUP BY / HAVING
- UNION系
- scalar / correlated subquery
- EXISTS / NOT EXISTS
- NOT IN + NULL
- ANY / ALL
- CTE / recursive CTE

## DB-09〜10 トランザクション・回復

- ACID
- 共有 / 専有ロック
- デッドロック
- dirty / non-repeatable / phantom read
- isolation levels
- 2相ロック
- WAL
- UNDO / REDO
- checkpoint
- rollforward
- full / differential / incremental backupを補完

## DB-11〜12 性能・DB機能

- B-tree / hash / composite index
- 選択率 / カーディナリティ
- full scan
- execution plan
- N+1
- view / materialized view
- procedure / trigger / cursor
- placeholder / bind variable

## DB-13〜14 分散・分析

- replication / sharding
- CAPを「pick any 2」の雑な暗記にしない説明へ修正
- NoSQL各モデル
- 2相コミットを補完
- OLTP / OLAP
- DWH / data mart / data lake
- ETL / ELT
- star schema / fact / dimension
- slice / dice / drill-down / roll-upを補完

# 8. Databaseページを学習ハブ化

更新:

`html/database.html`

変更:

- 旧「229語単語帳」を主画面にしない。
- DB-01〜14を4グループで上部表示。
- 229/229移行済みを表示。
- 監査結果を表示。
- 旧229語は検索・☆・既存進捗用の索引へ降格。
- 旧ナビでAlgorithm/System/Managementが準備中扱いになっていた古い表示を修正。

# 9. Database manifest更新

更新:

`database-terms-manifest.json`

- `audit-and-lesson-migration-complete`
- auditFile追加
- DB-01〜14を主教材にする方針を記録
- 旧11カテゴリを正式分類ではなく互換用表示カテゴリと明示

# 10. Lesson index

`json/lessons/lesson-index.json`

Database 14Lessonをorder 501〜514で登録。

構造化Lesson総数:

**35 → 49本**

# 11. Database CI強化

更新:

`tests/validate-audits.mjs`

追加した検証:

- Database terms 229件
- audit `termsAudited=229`
- assignmentGroups存在
- core/supporting/merge集計
- 全229 IDを監査上ちょうど1回割当
- 全229 IDを実装Lessonへちょうど1回割当
- 監査指定lessonIdと実際のlessonId一致
- unitId=`database`
- IPA中分類9一致
- 未割当 / 重複 / extra ID禁止

初回CIで `term-db-018 中間表` の `legacyTermIds` 登録漏れを検出した。

修正:

`DB-02` の `legacyTermIds` へ `term-db-018` を追加。

教材本文自体には中間表の説明・問題が既に存在しており、移行メタデータだけの漏れだった。

# 12. トップ・学習マップ更新

更新:

- `index.html`
- `json/curriculum/ap-2026-map.json`

現在表示:

- 構造化Lesson 49本
- 旧移行済み 441語
- Database 229/229

Databaseユニットは `existing-needs-audit` から `partial` へ変更。

完成には過去問照合・本番型SQL/ER/正規化演習などがまだ必要なため、complete扱いにはしていない。

# 13. 現在の移行進捗

| 分野 | 語数 | 移行 |
|---|---:|---:|
| Algorithm | 65 | 65/65 |
| Database | 229 | 229/229 |
| System | 75 | 75/75 |
| Management | 72 | 72/72 |
| Network | 480 | 未監査 |
| Security | 501 | 未監査 |

**441/441の監査済み旧語を実Lessonへ一意割当済み。**

# 14. 次の作業

大規模未監査は残り2つ。

1. Network 480語
2. Security 501語

Networkは通信フロー・構成図・サブネット計算・障害切り分け中心へ再構成する。

Securityは攻撃→成立条件→観測→対策、暗号/認証、リスク、実装、過去問対応へ再構成する。

# 15. 未完了・未確認

未完了:

- Network 480語監査
- Security 501語監査
- UI/情報メディア等の未整備ユニット
- 本番型問題データ
- Lesson正答履歴の永続保存

実ブラウザで未確認:

- DB-01〜14全ページ
- Database hubのPC/スマホ表示
- 全確認問題クリック
- 表/図横スクロール
- ダークモード

CI/Pagesは最終mainで改めて確認し、失敗時は完了扱いにしない。
