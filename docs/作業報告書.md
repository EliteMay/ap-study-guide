# AP Study Notes 作業報告書

GitHub `EliteMay/ap-study-notes` を正本とする変更記録です。

## 運用ルール

- ChatGPTからGitHubを直接確認・修正する。
- Codex / ZIPを通常フローにしない。
- UIや構成は改善のため変更可能。
- localStorage、用語ID、manifest、過去問対応はデータ消失を避けて変更する。
- 未確認事項を確認済みと書かない。

---

# 1. 教材監査の出発点

旧サイトはWebアプリとしては利用可能だったが、教材品質を再評価した結果、完成判定を撤回した。

確認した主な問題:

- 解説のテンプレート比率が高い。
- 旧6分野だけではAP全範囲をカバーしていない。
- 分野間に重複・誤配置候補がある。
- 1422語という語数が品質指標になっていた。
- 計算・SQL・コード・図・ケース問題が弱い。
- 現行4択は用語理解チェックであり、本番力測定ではない。

---

# 2. Phase 1: カリキュラム再設計

基準:

IPA「応用情報技術者試験 シラバス Ver.7.2」。

追加:

- `json/curriculum/ap-2026-map.json`
- `html/roadmap.html`
- `js/roadmap.js`
- `css/roadmap.css`

内部では9大分類・23中分類、表示では13学習ユニットへ整理した。

---

# 3. Phase 2-1: 旧アルゴリズム65語の全件監査

監査:

`json/curriculum/audits/algorithm-audit.json`

結果:

| action | 件数 |
|---|---:|
| keep-core | 31 |
| keep-supporting | 5 |
| merge-into-lesson | 25 |
| move-primary-unit | 4 |

既存IDを削除せず、Lessonへ移行する方針を採用した。

---

# 4. 構造化Lesson基盤

追加:

- `html/lesson.html`
- `js/lesson.js`
- `css/lesson.css`
- `json/lessons/lesson-index.json`

現在対応するsection type:

- text
- comparison
- diagram
- code-trace
- steps
- mistakes

確認問題は `checks[]` で管理する。

Lessonナビは同じ `unitId` 内をorder順で自動移動し、別ユニットへの移動は `next[]` で明示する方式へ変更した。

---

# 5. 実装済みアルゴリズムLesson

## ALG-01 アルゴリズムの表現と擬似言語トレース

置換:

- term-alg-001〜003

主内容:

- 入力 / 処理 / 出力
- 代入 / 分岐 / 反復 / 配列
- 逐次トレース

## ALG-02 計算量とオーダの読み方

置換:

- term-alg-004〜012

主内容:

- O(1), O(log n), O(n), O(n log n), O(n²)
- 処理回数からオーダを読む
- 時間 / 空間計算量

## ALG-03 配列・リストとデータの持ち方

置換:

- term-alg-014〜017

追加補完:

- 動的配列
- 多次元配列
- 循環リスト

## ALG-04 スタック・キューと操作順

置換:

- term-alg-018〜020

追加補完:

- push / pop
- enqueue / dequeue
- 循環キュー
- overflow / underflow

## ALG-05 ハッシュ表と衝突処理

置換:

- term-alg-021〜025

主内容:

- ハッシュ値計算
- collision
- chaining
- open addressing
- 平均O(1) / 最悪O(n)

## ALG-06 探索アルゴリズム

置換:

- term-alg-013
- term-alg-026
- term-alg-027

主内容:

- 線形探索
- 二分探索
- 整列済み前提
- left / mid / right
- 番兵法

## ALG-07 ソートを比較して理解する

置換:

- term-alg-028〜036

主内容:

- bubble / selection / insertion
- quick / merge / heap
- stable sort
- internal / external sort
- Shell sortを不足内容として補完

## ALG-08 再帰と問題分割の考え方

置換:

- term-alg-037〜044

主内容:

- 再帰呼出し
- 基底条件
- call stack
- divide and conquer
- dynamic programming
- memoization
- greedy
- backtracking

## ALG-09 木構造と走査

置換:

- term-alg-045〜051

追加補完:

- complete binary tree
- balanced tree
- AVL tree
- B-tree
- preorder / inorder / postorder

## ALG-10 グラフ探索と最短経路

置換:

- term-alg-052〜059

主内容:

- DFS / BFS
- directed / undirected / weighted graph
- shortest path
- Dijkstra
- Bellman-Fordを不足内容として補完

## ALG-11 文字列探索

置換:

- term-alg-065

追加補完:

- naïve search
- KMP
- BM

---

# 6. プログラミング・プログラム言語の穴を新規教材化

旧65語はデータ構造・アルゴリズムへ偏っていたため、IPA中分類2の不足部分を新規Lesson化した。

## PROG-01 プログラムの基本制御とデータ型

置換:

- term-alg-064 ビットマスク

追加:

- 順次 / 選択 / 反復
- integer / real / Boolean / string
- 型変換
- for / while
- bit maskの確認/設定/反転/解除

## PROG-02 関数・モジュール・引数・スコープ

追加:

- function / procedure / subroutine
- actual / formal argument
- pass by value / reference
- local / global
- scope / lifetime
- automatic / static variable
- high cohesion / low coupling

## PROG-03 プログラミング言語の分類と特徴

追加:

- procedural / OOP / functional / logic / scripting
- compile / interpreter / JIT
- BNF / EBNF
- static / dynamic typing
- stack / heap
- garbage collection

## PROG-04 Web・マークアップ・データ記述言語

追加:

- HTML
- CSS
- XML
- XHTML
- JSON
- YAML
- XSL / XSLT
- UML

---

# 7. 誤配置4語を基礎理論へ移動

旧アルゴリズムの以下4語は監査で `move-primary-unit` と判定していた。

- term-alg-060 AND
- term-alg-061 OR
- term-alg-062 XOR
- term-alg-063 ビットシフト

新規:

`json/lessons/foundation/fnd-01-bit-logic.json`

Lesson:

**FND-01 論理演算とビットシフト**

主内容:

- AND / OR / XOR truth table
- bitwise演算
- left / right shift
- 固定bit幅
- logical / arithmetic shiftの注意

PROG-01のビットマスクから関連付ける。

---

# 8. 旧アルゴリズム65語の移行完了

現在、旧65語は **65 / 65語すべて** が重複なしで1つの構造化Lessonへ割り当て済み。

内訳:

- ALG-01〜11
- PROG-01
- FND-01

PROG-02〜04は旧語の置換ではなく、不足シラバスを新規補完するLesson。

既存用語IDと `algorithm-terms-checked` は維持する。

この状態は「旧65語のテンプレ教材からの移行完了」を意味する。アルゴリズム分野の最終品質完成を意味しない。

---

# 9. アルゴリズムページを学習ハブ化

更新:

`html/algorithm.html`

変更:

- ALG-01〜11を分類して表示。
- PROG-01〜04を表示。
- FND-01を「基礎理論へ移した内容」として表示。
- 65/65割当済みを明示。
- 旧65語は検索・復習・進捗互換用の索引へ降格。
- 生成詳細を主教材として扱わない。

---

# 10. CI強化

更新:

`tests/validate.mjs`

従来チェック:

- 全JSON構文
- 9大分類 / 23中分類 / 13ユニット
- 65 audit decision
- Lesson ID / order / unit
- section type
- diagram構造
- checks
- 旧term重複置換
- 既存manifest/details/past/html refs

追加:

**旧アルゴリズム65語が65/65ちょうど1回ずつLessonへ割り当てられていることを必須化。**

未割当または重複割当があればCIを失敗させる。

---

# 11. 現在のLesson数

`json/lessons/lesson-index.json`

合計 **16Lesson**。

- algorithm-programming: 15
- foundation-theory: 1

---

# 12. 次の大バッチ

次はテンプレ生成比率が高く、分類混在も確認済みの次の2分野をまとめて監査する。

- system: 75語
- management: 72語

合計147語。

主な確認点:

- RFPをシステム企画/調達へ移すか。
- SLAをサービスマネジメントへ移すか。
- 開発技術とプロジェクト管理の境界。
- Management内に混ざるサービス管理項目。
- 似た用語カードを何本のLessonへ統合すべきか。

---

# 13. 未確認

この作業環境では公開GitHub Pagesを通常ブラウザとして全操作するE2E確認は未実施。

未確認:

- 16LessonのPC/スマホ実表示
- 全確認問題のクリック操作
- 図/表の横スクロール
- ダークモード視認性

CIとPages buildは最新mainで確認し、失敗した場合は完成扱いにしない。
